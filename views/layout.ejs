<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width,initial-scale=1"/>
    <title><%= title || 'App' %></title>

    <script src="https://cdn.tailwindcss.com"></script>

    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

    <style>
        body { font-family: 'Inter', sans-serif; }
    </style>

</head>
<body class="bg-gray-100 min-h-screen flex flex-col">

<header class="app-header bg-white shadow-sm sticky top-0 z-50">
    <nav class="nav container mx-auto px-4 sm:px-6 lg:px-8 flex items-center justify-between h-16">
        <a href="/" class="brand text-xl font-bold text-indigo-600 hover:text-indigo-800">CampusAuction</a>

        <div class="links hidden sm:flex sm:items-center sm:space-x-4">
            <a href="/community" class="px-3 py-2 rounded-md text-sm font-medium text-gray-700 hover:bg-gray-100 hover:text-gray-900">Community</a>
            <a href="/board" class="px-3 py-2 rounded-md text-sm font-medium text-gray-700 hover:bg-gray-100 hover:text-gray-900">자유게시판</a>
            <a href="/community/auction" class="px-3 py-2 rounded-md text-sm font-medium text-gray-700 hover:bg-gray-100 hover:text-gray-900">경매</a>
            <a href="/community/buy-sell" class="px-3 py-2 rounded-md text-sm font-medium text-gray-700 hover:bg-gray-100 hover:text-gray-900">사고팔고</a>
            <a href="/community/chat" class="px-3 py-2 rounded-md text-sm font-medium text-gray-700 hover:bg-gray-100 hover:text-gray-900">수다방</a>
            <a href="/rankings" class="px-3 py-2 rounded-md text-sm font-medium text-yellow-600 hover:bg-yellow-50 hover:text-yellow-700">🏆 랭킹</a>

            <% if (typeof currentUser !== 'undefined' && currentUser) { %>
                <% if (typeof isAdmin !== 'undefined' && isAdmin) { %>
                    <a href="/admin" class="px-3 py-2 rounded-md text-sm font-medium text-red-600 hover:bg-red-50 hover:text-red-700">관리자</a>
                <% } %>
                    <a href="/profile" class="flex items-center space-x-2 px-3 py-1 rounded-md text-sm font-medium text-gray-700 hover:bg-gray-100">
                        <% if (currentUser.profileImagePath) { %>
                            <% const filename = currentUser.profileImagePath.split('/').pop(); %>
                            <img class="h-6 w-6 rounded-full object-cover" src="/profile/avatar/<%= filename %>" alt="Profile">
                        <% } else { %>
                                <span class="inline-block h-6 w-6 overflow-hidden rounded-full bg-gray-100">
                                    <svg class="h-full w-full text-gray-300" fill="currentColor" viewBox="0 0 24 24">
                                        <path d="M24 20.993V24H0v-2.996A14.977 14.977 0 0112.004 15c4.904 0 9.26 2.354 11.996 5.993zM16.002 8.999a4 4 0 11-8 0 4 4 0 018 0z" />
                                    </svg>
                                </span>
                            <% } %>
                            <span><%= currentUser.anonymousNickname %></span> <%# 닉네임 표시 %>
                    </a>
                <a href="/profile" class="px-3 py-2 rounded-md text-sm font-medium text-gray-700 hover:bg-gray-100 hover:text-gray-900">마이페이지</a>
                <a href="/auth/logout" class="px-3 py-2 rounded-md text-sm font-medium text-gray-700 hover:bg-gray-100 hover:text-gray-900">로그아웃</a>
            <% } else { %>
                <a href="/auth/login" class="px-3 py-2 rounded-md text-sm font-medium text-indigo-600 hover:bg-indigo-50 hover:text-indigo-700">로그인</a>
            <% } %>
        </div>

        <%# 검색 폼 %>
        <div class="flex items-center ml-4">
            <form action="/search" method="GET" class="relative">
                <input type="text" name="q" placeholder="검색..." required aria-label="Search" class="block w-full sm:w-48 pl-3 pr-10 py-1.5 border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 text-sm" />
                <button type="submit" aria-label="Search submit" class="absolute inset-y-0 right-0 flex items-center justify-center w-10 text-gray-400 hover:text-indigo-600 focus:outline-none focus:text-indigo-600">
                    <svg class="h-4 w-4" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M8 4a4 4 0 100 8 4 4 0 000-8zM2 8a6 6 0 1110.89 3.476l4.817 4.817a1 1 0 01-1.414 1.414l-4.816-4.816A6 6 0 012 8z" clip-rule="evenodd" /></svg>
                </button>
            </form>
        </div>

        <a href="/notifications" class="p-1 rounded-full text-gray-400 hover:text-gray-500 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" title="알림">
            <span class="sr-only">알림 보기</span>
            <!-- 종 아이콘 SVG -->
            <svg class="h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9" />
            </svg>
        </a>
    </nav>
</header>

<%# 본문 내용 (flex-grow로 푸터를 맨 아래로 밀어냄) %>
<main class="container mx-auto px-4 sm:px-6 lg:px-8 py-10 flex-grow">

    <%# 플래시 메시지 (Tailwind 스타일) %>
    <% if (error && error.length > 0) { %>
        <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-4 rounded-md" role="alert">
            <p class="font-bold">오류</p>
            <p><%= error[0] %></p>
        </div>
    <% } %>
    <% if (success && success.length > 0) { %>
        <div class="bg-green-100 border-l-4 border-green-500 text-green-700 p-4 mb-4 rounded-md" role="alert">
            <p class="font-bold">성공</p>
            <p><%= success[0] %></p>
        </div>
    <% } %>

    <%- body %>
</main>

<%# 푸터 (mt-auto로 맨 아래 고정) %>
<footer class="app-footer bg-gray-800 text-white text-center py-4 mt-auto">
    <small>© CampusAuction Demo</small>
</footer>

<%# ⭐️ [확인!] Bootstrap JS 링크는 이제 필요 없으니 삭제하세요 (모달 제외) %>
<%# <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script> %>
<%# (단, profile.ejs의 탈퇴 모달 기능을 쓰려면 Bootstrap JS가 필요하긴 합니다.) %>
<%# (만약 Bootstrap JS가 필요하다면, 아래처럼 추가) %>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>